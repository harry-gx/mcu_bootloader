# S32K348 异常中断保护与异常回溯方案

## 1. 项目背景

在嵌入式系统中，异常中断（Fault Exception）通常意味着：

- 非法内存访问
- 栈溢出
- 非对齐访问
- 除 0
- 指针异常
- Flash/ECC 错误
- MPU 保护错误
- 非法函数跳转
- RTOS 栈破坏
- 野指针

对于量产产品、Bootloader、车载控制器、适航软件等系统：

> “异常发生后能够保存现场并定位问题”

是系统可靠性的重要组成部分。

因此，需要建立一套完整机制：

`异常检测 -> 现场保存 -> 软件复位 -> 异常记录持久化 -> ELF 回溯分析 -> 问题定位`

## 2. 方案目标

| 功能 | 说明 |
|---|---|
| 异常捕获 | 捕获 Cortex-M7 Fault |
| 现场保存 | 保存 CPU 关键寄存器 |
| 自动复位 | 异常后系统自动恢复 |
| 异常回溯 | 定位异常代码位置 |
| 调用链分析 | 分析异常前函数调用 |
| 日志保存 | 支持 noinit / D-Flash |
| 量产支持 | 支持现场问题追踪 |

## 3. Cortex-M7 异常体系

S32K348 基于 ARM Cortex-M7，主要 Fault 如下：

| 异常 | 作用 |
|---|---|
| MemManage | MPU/非法内存 |
| BusFault | 总线错误 |
| UsageFault | CPU 使用错误 |
| HardFault | 未处理 Fault |
| NMI | 不可屏蔽中断 |

## 4. 总体架构设计

```text
系统运行
  ↓
发生异常
  ↓
进入 Fault Handler
  ↓
提取 CPU 现场
  ↓
保存异常记录
  ↓
软件复位
  ↓
启动后读取异常记录
  ↓
ELF 回溯定位
  ↓
分析根因
```

## 5. 异常现场保存方案

### 5.1 保存内容

建议保存：

```c
typedef struct
{
    uint32_t magic;

    uint32_t exc_type;

    uint32_t r0;
    uint32_t r1;
    uint32_t r2;
    uint32_t r3;
    uint32_t r12;

    uint32_t lr;
    uint32_t pc;
    uint32_t xpsr;

    uint32_t msp;
    uint32_t psp;

    uint32_t exc_return;

    uint32_t cfsr;
    uint32_t hfsr;
    uint32_t dfsr;
    uint32_t afsr;

    uint32_t mmfar;
    uint32_t bfar;

    uint32_t shcsr;
    uint32_t control;

    uint32_t crc32;

} exc_record_t;
```

### 5.2 关键字段说明

| 字段 | 作用 |
|---|---|
| PC | 异常发生位置 |
| LR | 函数返回地址 |
| xPSR | CPU 状态 |
| MSP | 主栈指针 |
| PSP | 线程栈指针 |
| CFSR | Fault 详细状态 |
| BFAR | BusFault 地址 |
| MMFAR | MemFault 地址 |
| EXC_RETURN | 异常来源 |

### 5.3 保存位置

推荐：

1. 第一阶段：保存到 `noinit RAM`
2. 第二阶段：复位后写入 `D-Flash`

不建议：

- 异常中断中直接写 Flash

原因：

- Flash 驱动可能不可重入
- ECC 错误可能再次触发 Fault
- 异常时系统状态不可信

### 5.4 noinit 段设计

LD 文件：

```ld
.exc_noinit (NOLOAD) :
{
    . = ALIGN(8);

    __EXC_STORE_START = .;

    KEEP(*(.exc_noinit))

    . = ALIGN(8);

    __EXC_STORE_END = .;

} > int_sram
```

变量定义：

```c
__attribute__((section(".exc_noinit")))
volatile exc_record_t g_exc_record;
```

## 6. 异常回溯分析方法（重点）

本章节是整个方案最核心的部分。

目标：

1. 哪里发生异常
2. 为什么异常
3. 异常前调用了谁
4. 哪个函数导致
5. 是否栈破坏

### 6.1 Cortex-M7 异常现场原理

异常发生时，CPU 会自动压栈：

- R0
- R1
- R2
- R3
- R12
- LR
- PC
- xPSR

因此：

- `PC` = 异常发生时正在执行的位置
- `LR` = 异常前函数返回地址

这是回溯最重要的依据。

### 6.2 回溯总体流程

1. 读取异常记录
2. 查看异常类型
3. 查看 CFSR
4. 查看 PC
5. 使用 ELF 定位源码
6. 查看 LR
7. 分析函数调用链
8. 分析栈是否损坏
9. 结合 BFAR/MMFAR 定位非法地址

### 6.3 第一步：读取异常记录

启动后：

```c
if (g_exc_record.magic == EXC_MAGIC)
{
    exception_analyze();
}
```

建议打印：

- PC
- LR
- CFSR
- BFAR
- MMFAR
- MSP
- PSP

例如：

```text
PC     = 0x00412344
LR     = 0x00412001
CFSR   = 0x02000000
BFAR   = 0xFFFFFFFF
MMFAR  = 0x00000000
```

### 6.4 第二步：查看异常类型

| 异常 | 含义 |
|---|---|
| MemManage | MPU/栈越界 |
| BusFault | 非法地址 |
| UsageFault | CPU 错误 |
| HardFault | 未处理 Fault |

### 6.5 第三步：分析 CFSR

`CFSR = SCB->CFSR`，分三部分：

- `[7:0]` MMFSR
- `[15:8]` BFSR
- `[31:16]` UFSR

常见错误：

| 位 | 含义 |
|---|---|
| DIVBYZERO | 除 0 |
| UNALIGNED | 非对齐 |
| PRECISERR | 精确总线错误 |
| IMPRECISERR | 非精确总线错误 |
| INVPC | 非法 PC |
| INVSTATE | 非法状态 |
| UNDEFINSTR | 未定义指令 |
| DACCVIOL | 数据访问违规 |
| IACCVIOL | 指令访问违规 |

### 6.6 第四步：定位 PC

这是最关键步骤。

假设：`PC = 0x00412344`

方法 1：`addr2line`

```bash
arm-none-eabi-addr2line -e app.elf -f -a 0x00412344
```

输出：

```text
0x00412344
my_function
/home/project/app/src/test.c:123
```

得到：异常发生在 `test.c` 第 123 行，函数 `my_function()`。

### 6.7 第五步：查看反汇编

生成：

```bash
arm-none-eabi-objdump -d app.elf > app.dis
```

搜索：`00412344`

查看：

```text
412340: ldr r0, [r1]
412344: str r0, [r2]
412348: bl  foo
```

说明异常可能发生在 `str r0, [r2]`，进一步看 `r2` 是否非法。

### 6.8 第六步：查看 LR

`LR = 当前函数返回地址`

例如：`LR = 0x00412001`

注意：Thumb 模式最低位为 1，真实地址为 `0x00412000`。

定位 LR：

```bash
arm-none-eabi-addr2line -e app.elf -f -a 0x00412000
```

可得调用者函数与源码位置。

### 6.9 第七步：分析函数调用链

Cortex-M 调用原理：

- 函数调用 `A() -> B()` 会执行 `bl B`
- CPU 把返回地址写入 LR

因此：`LR = 调用者位置`

### 6.10 查看调用关系

例如：

- `PC = B 函数`
- `LR = A 函数`

则关系为：

```text
A()
  ↓
B()
  ↓
异常
```

### 6.11 使用反汇编分析调用链

例如：

```text
400100: bl foo
400104: mov r0, r1
```

说明 `foo` 返回后会执行 `0x400104`，因此 `LR = 0x400105`。

### 6.12 栈回溯（高级）

如果栈未破坏，可进一步分析 `MSP/PSP` 指向的栈内容。

栈中可能存在：

- 历史 LR
- 历史 PC

调试器查看：

- JLink：Memory Window 查看 MSP/PSP
- GDB：

```gdb
x/32wx 0x2040FFF0
```

### 6.13 如何判断栈损坏

| 现象 | 原因 |
|---|---|
| PC 乱码 | 栈破坏 |
| LR 异常 | 栈溢出 |
| MSP 越界 | 栈崩溃 |
| addr2line 无法定位 | 栈损坏 |

### 6.14 分析 BFAR/MMFAR

如果 `BFARVALID = 1`，则 `BFAR = 非法访问地址`。

例如：`BFAR = 0xFFFFFFFF`，说明访问了非法地址。

### 6.15 精确错误 vs 非精确错误

`PRECISERR`：

- 当前 PC 就是异常位置
- 定位友好

`IMPRECISERR`：

- 异常延后触发
- PC 不一定准确

通常与以下有关：

- 写缓冲
- DMA
- AXBS
- Cache

### 6.16 实际案例分析

案例 1：除 0

- `CFSR = DIVBYZERO`
- `PC = math.c:88`

结论：除数为 0。

案例 2：非法指针

- `BFAR = 0xFFFFFFFF`
- `PRECISERR = 1`

说明：野指针。

案例 3：函数指针异常

- `INVPC = 1`

说明：函数跳转地址非法。

案例 4：栈溢出

- `MSP` 越界
- `PC` 无法定位

说明：栈已破坏。

## 7. 验证方案

### 7.1 除 0 测试

```c
volatile int a = 1;
volatile int b = 0;
volatile int c = a / b;
```

### 7.2 非对齐访问

```c
volatile uint32_t *p = (uint32_t *)0x20000001;
volatile uint32_t v = *p;
```

### 7.3 非法地址访问

```c
volatile uint32_t *p = (uint32_t *)0xFFFFFFFF;
*p = 0x12345678;
```

### 7.4 非法函数跳转

```c
typedef void (*func_t)(void);
func_t f = (func_t)0xFFFFFFFF;
f();
```

### 7.5 栈溢出测试

```c
void test(void)
{
    uint8_t buf[1024];
    test();
}
```

